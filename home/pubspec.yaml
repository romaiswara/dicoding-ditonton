name: home
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.5

  core:
    path: ../core
  movie:
    path: ../movie
  tv_series:
    path: ../tv_series

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

dependency_overrides:
  http: ^1.0.0

flutter:
